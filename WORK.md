# WORK — lyra-ds/blade

## In progress
- [ ] Ondas B–F sobre o `@lyra-ds/alpine` 0.2.0 — plano aprovado em `.batuta/plan-alpine-0-2-ondas-b-f.md` (34 tasks, 23 componentes, releases 0.4.0→0.8.0). **Onda B fechada** (5 componentes, suíte 601 → 674). Ondas B e C fechadas de ponta a ponta (releases 0.4.0 e 0.5.0, 2026-08-09). Próxima: onda D (tasks 19–24, datas). Task 7 (`app-sidebar`) **DESTRAVADA em 2026-08-09**: o upstream atendeu a spec `docs/spec-alpine-app-sidebar.md` e o `lyraAppSidebar` está publicado no `@lyra-ds/alpine` 0.3.0 (verificado no `dist/index.js` do tarball npm, não só no fonte) — contrato `collapsed` modelable + `defaultCollapsed`/`width`/`labels`, bindings `root` e `toggle`, chevron servido pelo consumidor com `x-show`. Onda D também não tem bloqueio: `lyraCalendar`, `lyraDatePicker`, `lyraDateRangePicker` e `lyraTimePicker` já saem no 0.3.0

## Backlog
- [x] Upstream (styles): hover vence o active no SegmentedControl — RESOLVIDO em 2026-08-09. O fix saiu no lyra (`447c70c`) e foi publicado no `@lyra-ds/styles` 0.4.2. Como o demo já pedia `^0.4.1`, não houve edição de dependência: o `package.json` ficou intacto e só o lockfile mudou — `npm install` não bastava (o lock fixava 0.4.1), foi preciso `npm update @lyra-ds/styles`. Verificado no bundle construído que a regra específica `.lyra-segmented__option--active:hover` agora vence o hover genérico; build ok e `artisan test` 3/3. Commit `176ba3f` no blade-demo. O blade não foi tocado, como previsto
- [ ] Upstream (alpine): bind `list` do `lyraTimePicker` aceitar `aria-label` traduzível pelas options da factory — hoje é hardcoded `Time options`, o que impediu expor `labels.timeOptions` no Blade (uma prop que funcionaria no HTML servido e seria revertida no boot do Alpine é pior que prop nenhuma; decisão 2026-08-09 na task 22)
- [ ] Upstream (alpine): `lyraTheme()` aceitar storage key configurável — destrava o argumento opcional do @lyraThemeScript (v1 ficou zero-config; decisão 2026-08-09 durante a task 16)

- [ ] Fase 2 — ondas B–F por `docs/spec-alpine-proximas-ondas.md` (2026-08-08): B = 3 estáticos + 5 bindings baratos (sem upstream pesado); C = médios + infra (bottom-sheet, theme store, scroll-spy); D = calendar → pickers (BottomSheet antes, decisão fechada); E = combobox → time-zone-picker, command-palette; F = data-table (sort dual-mode, default server-side) + promovidos. Adiados: recurrence-selector (promover pós-onda D), weekly-schedule-editor, slot-picker, calendar-view. Excluído: create-workspace-dialog (vira receita na doc). Bindings novos nascem no monorepo lyra (packages/alpine) antes de cada onda Blade

- [x] Task 23: seção "Dates and times" na galeria do blade-demo — os 4 componentes da onda D numa narrativa única de cronograma de release (deploy, janela de observação, manutenção) [batuta/20260809-demo-datas] → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-e887f17b2fdb56b9`, commit `eba565b` no demo. 70 linhas, só adições. Maestro verificou no browser, e não só por comando: esses componentes não renderizam nada sem Alpine (são todos `x-if`/`x-for` pelas exceções declaradas), então um gate só de `artisan test` passaria com a seção vazia — DOM real confirmou 168 células de dia (4 × 42), cabeçalhos por `Intl`, triggers com o separador ` – ` do range, os 3 inputs hidden com a convenção de array, 8 opções de horário e 0 erros de console; em viewport de 500px os 3 popovers viram bottom-sheets ao vivo, sem reload. Desvio declarado: sem worktree, direto na main do demo (precedente das varreduras anteriores) (2026-08-09)
## Done
- [x] Task 22: componente time-picker sobre o binding lyraTimePicker (alpine 0.3.0) — fecha os componentes da onda D [batuta/20260809-142453-time-picker] → codex (gpt-5.6-sol, reasoning high) via Compozy. Implementado com trigger de relógio, listbox responsivo e estado modelable; na rodada corretiva, o achado aceito passou a omitir `step` inválido antes do x-data, enquanto o achado sobre `aria-selected="false"` foi recusado para preservar o binding canônico e ARIA válido. A suíte completa passou a 873 testes, com Pint limpo e guidelines idempotentes. (2026-08-09)
- [x] Task 21: componente date-range-picker sobre o binding lyraDateRangePicker (alpine 0.3.0) — fecha o par de pickers de data da onda D [batuta/20260809-140705-date-range-picker] → codex (gpt-5.6-sol, reasoning high) via Compozy. Entregue com seleção `{start,end}`, calendars em modo range, ramos responsivos com trigger acessível único e dois inputs hidden (`name[start]`/`name[end]`) sincronizados pelo `lyra:change`; suíte 820 → 844. As lições da task 20 entraram no brief de saída (trigger sem wrapper via `wrap-trigger`, presença de campo por `!== null && !== ''`) e o cross-review veio limpo nas 3 lentes (`sess-5695c369bc0b84d0`) — primeira task da onda sem rodada corretiva. Decisão do maestro: duplicar o template em vez de fatorar parcial compartilhado (o React duplica; parcial acoplaria dois componentes públicos). Desvio declarado: `peer disconnected` mid-TDD pela 3ª vez seguida na onda, depois de escrever tudo e antes de commitar; maestro verificou (844/844, 4018 assertions, pint limpo, gerador idempotente) e commitou (2026-08-09)
- [x] Task 20: componente date-picker compondo calendar + popover/bottom-sheet sobre o binding lyraDatePicker (alpine 0.3.0) [batuta/20260809-133730-date-picker] → codex (gpt-5.6-sol, reasoning high) via Compozy. Entregue com ramos responsivos, aliases Alpine, submit nativo opt-in e paridade de classes/ARIA com o React; na rodada corretiva, os dois achados aceitos do cross-review preservaram `"0"` em label/hint/error e eliminaram o trigger interativo aninhado, com ampliação de escopo autorizada para `popover`; a suíte passou a 820 testes. (2026-08-09)
- [x] Task 19: componente calendar sobre o binding lyraCalendar (alpine 0.3.0) — alicerce da onda D, destrava date-picker e date-range-picker; suíte 781 → 796 [batuta/20260809-131640-calendar] → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-6c5b72fbc15ed823`. Paridade exata de classes e ARIA com o React, três views em `x-if`, slot `dayMarker` no lugar do `renderDayMarker` e serialização das props com `json_encode` + flags HEX (teste de regressão prova que `</script>` e quebra de aspas não escapam do literal). Decisões do maestro no brief: a grade vem de `x-for` (exceção declarada ao estático-primeiro — todo o texto da grade sai de `Intl` com o locale do consumidor e o pacote não vai ganhar `ext-intl`; servir locale errado é pior que servir vazio) e `isDateDisabled` não vira prop Blade, só `disabledDates`. Cross-review limpo nas 3 lentes (`sess-31004a6d518d2e21`), que também conferiu a composição futura pelos bindings date-picker/date-range-picker. Desvio declarado: a sessão morreu com `peer disconnected` mid-TDD (`health: dead`, sem elegibilidade para wake — a reconexão única do contrato não existia) depois de escrever os 5 arquivos e antes de commitar; maestro verificou integralmente (796/796, 3659 assertions, pint limpo, gerador idempotente, scope conferido) e commitou (2026-08-09)
- [x] Task 7: componente app-sidebar sobre o binding lyraAppSidebar (alpine 0.3.0) — dívida da onda B destravada; suíte 766 → 781 [batuta/20260809-122709-app-sidebar] → codex (gpt-5.6-sol, reasoning high) via Compozy, fast-forward `394078f` na main. 2 rodadas corretivas, ambas nascidas do cross-review: (1) `array_merge` deixava `labels` null zerar o aria-label servido do toggle enquanto o binding usa `??` — estático-primeiro quebrado; e `(int) $width` truncava `312.5`, furando paridade com o React. (2) a correção do width removeu o cast e abriu injeção de JS no `x-data` (`:width="1); window.pwned=1; //"` saía literal) — erro de julgamento do maestro ao aceitar a *forma* do finding em vez do comportamento exigido; fechado com `is_numeric`/`is_finite`/`json_encode` + teste de regressão. Cross-review final limpo nas 3 lentes. Quirk: as 3 sessões Compozy morreram com `peer disconnected` logo após commitar (report perdido; gate é mecânico e não dependeu dele) (trail: .batuta/runs/2026-08-09-app-sidebar-brief.md) (2026-08-09)
- [x] Vitrine dos toasts dinâmicos no blade-demo + bump @lyra-ds/alpine ^0.3.0 (release de hoje destravou) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-af7f35dd747fbac4`, commit `78702de` no demo. Seção Interactive Toasts (success/error/info com duration:0 persistente), toast-stack único, row em x-data. Maestro verificou (0.3.0 no dist, artisan test 3/3, npm build, view:cache) e integrou — sistema de toasts completo de ponta a ponta (trail: .batuta/runs/2026-08-09-demo-toasts-brief.md) (2026-08-09)
- [x] ToastStack com fila dinâmica Alpine (toasts com auto-dismiss; suíte 762 → 766) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-0607a1d117f4015a`, squash na main. 1 retry (sessão caiu mid-TDD; feedback de ordem de atributos do SVG). Estáticos intactos, template x-for com paridade, closeButton do binding. Vitrine no demo bloqueada até a próxima release do alpine (lyraToasts não está no 0.2.0). Também: fix do toggle de tema no demo (`2f96449` — diretivas sem x-data ancestral nunca são processadas; lição p/ verificação de demos interativos) + toggle icon-only (`4db5da0`); diagnóstico segmented-control = hover/focus sobrepondo --active no CSS do styles, idêntico no React, não é bug do blade (trail: .batuta/runs/2026-08-09-toast-stack-alpine-brief.md) (2026-08-09)
- [x] Task 18: Release 0.5.0 (PR #6) mergeada e publicada — ONDA C COMPLETA (TableOfContents, TimeInput, BottomSheet, FileUpload, FileManager, @lyraThemeScript; suíte 674 → 762). Checks verdes, merge por bypass de admin (precedente), tag v0.5.0 + Packagist via webhook (2026-08-09)
- [x] Task 17: galeria do blade-demo — 5 vitrines da onda C + @lyraThemeScript no head + toggle light/dark via $store.theme (nome real conferido no index.ts) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-b53aaea379c8be4d`, commit `dc91510` no demo. Maestro verificou (5/5, greps, artisan test 3/3, view:cache, npm build) e integrou. ACHADO: lyraToasts já registrado no alpine — PRD dos toasts implementado upstream, lado Blade destravado (trail: .batuta/runs/2026-08-09-demo-gallery-onda-c-brief.md) (2026-08-09)
- [x] Task 16: Infra de tema — diretiva @lyraThemeScript (onda C; suíte 755 → 762) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-de9d91ff702aa7c9`, commit squash na main. Stop condition LEGÍTIMA na 1ª tentativa (2ª da onda): theme.ts hard-coda STORAGE_KEY e a factory não aceita options — o argumento de chave custom era inviável; v1 fechada sem argumento, chave fixa lyra-theme em lockstep. CANDIDATA UPSTREAM registrada: lyraTheme() aceitar storage key configurável (aí a diretiva ganha o argumento). Maestro verificou (762/762, 3437 assertions, pint ok, script conferido: dataset.theme pré-paint, matchMedia p/ system, try/catch; README Theme ok) e integrou (trail: .batuta/runs/2026-08-09-theme-infra-brief.md) (2026-08-09)
- [x] Task 15: Componente FileManager (onda C; suíte 740 → 755) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-de14e54a3ec884f5`, commit squash na main. Primeira tentativa limpa, commit-antes-do-report cumprido. Duas árvores servidas (lista/grade) com data-name, dois x-modelable distribuídos (root→view, busca→query — limite de 1 por elemento), actions callable compondo o dropdown. Maestro verificou (755/755, 3419 assertions, pint ok, idempotente) e integrou (trail: .batuta/runs/2026-08-09-file-manager-brief.md) (2026-08-09)
- [x] Task 14: Componente FileUpload (onda C; suíte 727 → 740 — exceção documentada ao markup-servido: template x-for com paridade de classes) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-afe0a44f4b56eab4`, commit squash na main. Primeira tentativa limpa, commit-antes-do-report cumprido. Maestro verificou (740/740, 3212 assertions, pint ok, idempotente, 21 fixtures incl. árvore do item no template) e integrou (trail: .batuta/runs/2026-08-09-file-upload-brief.md) (2026-08-09)
- [x] Task 13: Componente BottomSheet (onda C; suíte 714 → 727) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-f0042f29785fd1c1`, commit squash na main. Stop condition LEGÍTIMA na 1ª tentativa: o brief do maestro mandava labelId no x-data, mas a factory do lyraBottomSheet só aceita defaultOpen (título/labelledby são servidos) — executor parou certo, fonte confirmou, esclarecimento na mesma sessão entregou limpo. Não conta na régua de escalada (erro de brief). Maestro verificou (727/727, 3079 assertions, pint ok, idempotente, wiring conferido) e integrou (trail: .batuta/runs/2026-08-09-bottom-sheet-brief.md) (2026-08-09)
- [x] Task 12: Componente TimeInput (onda C; suíte 685 → 714) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-33a1c42d63125d25`, commit `fd68ef2`. Primeira tentativa limpa, commit-antes-do-report cumprido. Maestro verificou (714/714, 2976 assertions, pint ok, idempotente; aria-valuetext default conferido literal contra o React "X hours and Y minutes"; split wire:model/nativos no padrão da casa) e integrou (trail: .batuta/runs/2026-08-09-time-input-brief.md) (2026-08-09)
- [x] Task 11: Componente TableOfContents (onda C; suíte 674 → 685) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-50605ff59fa56e30`, commit `4d2cf18`. Primeira tentativa limpa, commit-antes-do-report cumprido. Maestro verificou (685/685, 2816 assertions, pint ok, idempotente, classes conferidas contra o fonte React incl. lyra-toc__title) e integrou (trail: .batuta/runs/2026-08-09-table-of-contents-brief.md) (2026-08-09)
- [x] Task 10: Release 0.4.0 (PR #4) mergeada e publicada — onda B completa no changelog (7 feats: CodeBlock, CookieBanner, SegmentedControl, SidebarGroup, WorkspaceSwitcher, CheckboxGroup, RadioGroup + fix numeric-string). Checks 6×verde (L12/13 × PHP 8.3/8.4); merge por bypass de admin (precedente 0.3.0: política exige review e Copilot só comenta); tag v0.4.0 + Packagist via webhook (2026-08-09)
- [x] Task 9: galeria do blade-demo com os 5 da onda B + migração integral `<x-lyra::…>` → `<lyra:…>` (141 → 0 ocorrências, 36 slots intactos) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-86f288d3cdcddd60`, commit `252cbdb` no blade-demo. Protocolo commit-antes-do-report funcionou. Maestro verificou (greps, diff só-migração+adições, artisan test 3/3, view:cache ok) e integrou (trail: .batuta/runs/2026-08-09-demo-gallery-onda-b-brief.md) (2026-08-09)
- [x] Componente WorkspaceSwitcher (fase 2, onda B — ÚLTIMO DA ONDA) [batuta/20260809-020500-workspace-switcher] → codex (gpt-5.6-sol, reasoning high) via Compozy — suíte 657 → 674 testes (2735 assertions); alvo 16/16 (118 assertions), Pint limpo e gerador idempotente; ids ARIA servidos, create reutiliza `lyra:change` via `create-id` e o popover permanece no DOM com `x-cloak` condicional. Primeira sessão da noite que commitou sozinha — a queda veio depois do commit, só o report se perdeu (trail: .batuta/runs/2026-08-08-workspace-switcher.md) (2026-08-08)
- [x] Componente SegmentedControl (fase 2, onda B) [batuta/20260809-014800-segmented-control] → codex (gpt-5.6-sol, reasoning medium) via Compozy — suíte 644 → 657 testes (2615 assertions); alvo 12/12 (67 assertions), Pint limpo e gerador idempotente. Estado inicial servido com roving tabindex idêntico ao React; integração por `lyraSegmentedControl`, `x-modelable="value"` e evento `lyra:change`, sem API form-nativa inventada. Sessão caiu antes do commit (`peer disconnected`, 6ª seguida) → maestro verificou e commitou; desvio declarado (trail: .batuta/runs/2026-08-08-segmented-control.md) (2026-08-08)
- [x] Componente SidebarGroup (fase 2, onda B) [batuta/20260809-013500-sidebar-group] → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-00968fe4cc99c422` — suíte 628 → 644 testes (2546 assertions), 11 testes no componente. Itens ficam servidos no DOM com `x-show="!collapsed"` em vez do `x-if` do React (template inerte esconderia a navegação inteira sem Alpine); `x-cloak` só quando começa colapsado. Contrato para o `app-sidebar` (task 7): seleção é o evento `lyra:select` com `{ id }` lido do `data-id` servido — sem props de callback. Sessão caiu antes do commit (`peer disconnected`, 5ª seguida) → maestro verificou, escreveu a linha e commitou; desvio declarado. Roteado como complex (high) em vez do medium previsto no plano (trail: .batuta/runs/2026-08-08-sidebar-group.md) (2026-08-08)
- [x] Componente CookieBanner (fase 2, onda B) [batuta/20260809-011621-cookie-banner] → codex (gpt-5.6-sol, reasoning medium) via Compozy — suíte 617 → 628 testes (2454 assertions); alvo 10/10 (51 assertions), Pint limpo e gerador idempotente; `x-cloak` obrigatório mantém o banner oculto sem Alpine e ambos os botões servem `type="button"` antes da inicialização. Sessão `sess-504f84b5913c0866` caiu antes do commit (`peer disconnected`, 4ª seguida) → maestro verificou integralmente (render real conferido) e commitou; desvio declarado (trail: .batuta/runs/2026-08-08-cookie-banner.md) (2026-08-08)
- [x] Componente CodeBlock (fase 2, onda B) [batuta/20260809-005615-code-block] → codex (gpt-5.6-sol, reasoning medium) via Compozy — suíte 601 → 617 testes (2401 assertions); alvo 15/15 (77 assertions), Pint limpo e gerador idempotente; rodada corretiva serviu `type="button"` antes do Alpine iniciar (defeito causado pelo brief do maestro, não pelo modelo). Sessões `sess-ed1b870a8d21d800` + `sess-5a9a49762cf09953`, commit `34457c0`; `peer disconnected` 3× seguidas (uma antes do commit, uma depois) → verificação integral do maestro, incluindo render real conferido à mão (trail: .batuta/runs/2026-08-08-code-block.md) (2026-08-08)
- [x] Bump do `blade-demo` para `@lyra-ds/alpine` ^0.2.0 [batuta/20260809-004612-demo-alpine-bump] → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-7b056747d3ccf7fe`, commit `1313bb8` no repo do demo. Dois incidentes de runtime, nenhum do modelo: (1) bind recusado porque o provider `codex` quer o modelo **sem** o prefixo `codex/` — dispatch marcado `indeterminate`, resolvido como não-enviado por inspeção do histórico (só hooks, zero agent_message, árvore intacta) antes do re-dispatch; (2) `peer disconnected` pós-commit (8ª vez), report perdido nos três shapes conhecidos → verificação integral do maestro (0.2.0 instalado, build 161ms, 3/3 testes, view:cache ok, scope exato nos 2 arquivos, lockfile sem churn de terceiros). Achado: o pacote foi de 118 KB → 590 KB unpacked e `Alpine.plugin(lyra)` registra os 28 state machines de uma vez — sem tree-shaking para quem usa 7; decisão de doc/upstream levada para a onda C (trail: .batuta/runs/2026-08-08-demo-alpine-bump.md) (2026-08-08)
- [x] Matriz de compatibilidade do README para `@lyra-ds/alpine` `^0.2` + plano formal das ondas B–F → maestro (claude), commit `b57c0e3`. Desvio declarado da lane: linha de docs já escrita durante a investigação do release, não valia uma sessão de codex. `@lyra-ds/styles` continua em 0.4.1 e já traz `components/{scheduling,files,data}` — as classes da onda nova já existem (2026-08-08)
- [x] Bugfix Stack/Grid gap/columns numéricos string → SUPERSEDED pela PR #5 (`716c344`, outra sessão, fix mais amplo incl. skeleton height/width; mesmo root cause da sessão do demo). Ciclo local: 2 bloqueios de POLICY Compozy no worktree (não é falha de modelo) → fallback subprocess entregou fix idêntico verificado (red→green, 601/601), descartado no conflito de integração — árvore vence, main verificada verde. Lições: policy Compozy bloqueando worktrees novos (investigar); cache de views compiladas engana teste pós-stash (tocar os blades antes de re-rodar) (trail: .batuta/runs/2026-08-08-stack-grid-numeric-brief.md) (2026-08-08)
- [x] Varredura da galeria do demo COMPLETA (17 → 50 componentes, catálogo inteiro em seções temáticas) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-249be0b6d36e36a2`, commit `1e6ac09` no blade-demo. Primeira tentativa limpa. Maestro verificou (33/33 por grep, diff só-adições, artisan test 3/3, view:cache ok) e integrou (trail: .batuta/runs/2026-08-08-demo-gallery-sweep-brief.md) (2026-08-08)
- [x] Galeria do demo: seção Choice groups (CheckboxGroup + RadioGroup) → codex via Compozy `sess-fe5baa7c7347e7e0`, commit `1ea42fa` no blade-demo. Maestro verificou (artisan test 3/3, view:cache ok) e integrou (trail: .batuta/runs/2026-08-08-demo-choice-groups-brief.md) (2026-08-08)
- [x] Componente RadioGroup (fase 2, onda B, item 2/2 — ESTÁTICOS DA ONDA B COMPLETOS no blade; suíte 578 → 598) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-aa1e12001460a65a`, commit `228e2d3`. Primeira tentativa limpa, sessão sobreviveu ao turno. Maestro verificou (598/598, 2315 assertions, pint ok, idempotente) e integrou (trail: .batuta/runs/2026-08-08-radio-group.md) (2026-08-08)
- [x] Componente CheckboxGroup (fase 2, onda B, item 1/2 — estáticos; suíte 560 → 578) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-f7ffb36a7ee6c3fd`, commit `572993a`. Sessão morreu pós-commit (peer disconnected, 7ª vez — padrão estável); report perdido, verificação integral do maestro no lugar (578/578, 2246 assertions, pint ok, gerador idempotente). Adaptação declarada: prop `name` → `name="x[]"` form-nativo. Achado do scout: app-sidebar NÃO é estático (renderiza SidebarGroup) — bloqueado até sidebar-group existir (alpine onda B + blade) (trail: .batuta/runs/2026-08-08-checkbox-group.md) (2026-08-08)
- [x] Spec dos bindings Alpine para as próximas ondas (`docs/spec-alpine-proximas-ondas.md`): 24 interativos analisados fonte a fonte (4 agentes de análise em paralelo sobre packages/react), vereditos portar/adiar/excluir fechados com o usuário, 3 decisões de arquitetura fechadas (BottomSheet antes dos pickers; data-table sort dual-mode default server-side; theme vira Alpine.store + script inline no layout), grafo de dependências e 6 helpers de internal/ mapeados (2026-08-08)
- [x] Release 0.3.0 (PR #3) mergeado e publicado (2026-08-08): 7 interativos da onda core + groundwork Livewire + README de interatividade. Checks 4×verde; merge por bypass de admin autorizado pelo usuário (política exige review aprovada e Copilot só comenta); tag v0.3.0 + Packagist via webhook (2026-08-08)
- [x] README fase 2 (fechamento da onda): Interactivity real (setup Alpine + x-cloak + nota de inércia + Livewire/x-modelable) e matriz tripla (styles ^0.4, alpine ^0.1.1) → codex (gpt-5.6-sol) via Compozy `sess-d860c1ce8e135fac`, commit `8f50f3c` (desvio registrado: commit caiu na main em vez do worktree; aceito após revisão integral). Galeria do demo completa com os 7 interativos (2026-08-07)
- [x] Componente Popover (fase 2, onda core, item 7/7 — ONDA CORE COMPLETA: Dropdown, Dialog, Drawer, Tabs, Accordion, Tooltip, Popover em um dia; suíte 543 → 560 testes) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-886332ea6a7d5ab4`, commit `f3284a3`. Cross-review limpo nas 3 lentes. Maestro verificou (560/560, 2182 assertions, pint ok) e integrou (trail: .batuta/runs/2026-08-07-popover.md) (2026-08-07)
- [x] Componente Tooltip (fase 2, onda core, item 6/7 — data-tip/data-state servidos, slot embrulhado no target binding, sem estado controlável) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-5c20f949cc0d8dd5`, commit `66d8f3a`. Cross-review limpo nas 3 lentes (1º da onda). Maestro verificou (543/543, 2092 assertions, pint ok) e integrou (trail: .batuta/runs/2026-08-07-tooltip.md) (2026-08-07)
- [x] Componente Accordion (fase 2, onda core, item 5/7 — uncontrolled, x-modelable openItems, inert servido nos colapsados) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-b570fcc5e7aff02e` (retry na mesma sessão), commit `d0aa40f`. Cross-review: 1 Medium aceito (isolamento por item no teste — strpos single-line nunca casava). Maestro verificou (531/531, 2036 assertions, pint ok) e integrou (trail: .batuta/runs/2026-08-07-accordion.md) (2026-08-07)
- [x] Componente Tabs (fase 2, onda core, item 4/7 — root estrutural do cânone Alpine, tablist com a emissão lyra-tabs, extensão documentada de panel content) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-f5b19de99cf6f7c9` + retry `sess-7733a4cca5ce6f0f`, commit `f0ebefd`. Cross-review: 1 Medium aceito (array com chaves esparsas dessincronizava o active — array_values + regressão). Maestro verificou (514/514, 1903 assertions, pint ok) e integrou (trail: .batuta/runs/2026-08-07-tabs.md) (2026-08-07)
- [x] Componente Drawer (fase 2, onda core, item 3/7 — irmão do Dialog: só defaultOpen/labelId no x-data, Esc/backdrop sempre fecham) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-111d65594e2f8ad4`, commit `9e94cb2`. 1ª tentativa limpa mas sem commit (maestro commitou o WIP). Cross-review via sessão Compozy: 3 lentes limpas, 1 High recusado (artefato de baseline do diff). Maestro verificou (496/496, 1798 assertions, pint ok) e integrou (trail: .batuta/runs/2026-08-07-drawer.md) (2026-08-07)
- [x] Componente Dialog (fase 2, onda core, item 2/7): overlay/panel/header/close/body/footer com paridade exata, wire:model desviado para o root modelable, closable/labelId como decisões fechadas → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-f0159cdae78c4064`, commit `d3a0fa3`. Cross-review: 2 aceitas (atributos fixos do panel antes do bag — 2ª vez que a lição de duplicatas aparece; ENT_SUBSTITUTE no labelId), Minimalist limpo. Maestro verificou (482/482, 1707 assertions, pint ok, gerador idempotente, worktree e main) e integrou (trail: .batuta/runs/2026-08-07-dialog.md) (2026-08-07)
- [x] Demo Laravel app (`~/Projects/lyra-ds/blade-demo`): Laravel 13.24 + lyra-ds/blade via path-repo symlink + @lyra-ds/styles 0.4.1 + @lyra-ds/alpine 0.1.0, galeria em `/` com 26 componentes e Dropdown interativo → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-33f098e53fe5f4bc`, commit `430c0cc` no repo do demo. Maestro verificou (artisan test 3/3, npm build ok, symlink real) e integrou. Achado: styles não embarca regra [x-cloak] — candidata a doc/styles (trail: .batuta/runs/2026-08-07-blade-demo.md) (2026-08-07)
- [x] Componente Dropdown (fase 2, onda core, item 1/7 — PRIMEIRO INTERATIVO, padrão Alpine estabelecido: markup inicial + x-data/x-bind do @lyra-ds/alpine, x-modelable, x-cloak condicional, sem ids server-side) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-1d654fe78f786326` + retry `sess-29d0305cdd1a0c9e`, commit `56f130d`. Cross-review 3 lentes: 3 aceitas (ordem do x-data vs bag; fixtures root-only p/ guidelines; asserção Livewire no root) corrigidas no retry, 2 recusadas com justificativa (dedup falso; coerção de align no precedente). Maestro verificou (468/468, 1616 assertions, pint ok, gerador idempotente, worktree e main) e integrou (trail: .batuta/runs/2026-08-07-dropdown.md) (2026-08-07)
- [x] Groundwork Livewire (fase 2, pré-requisito): livewire/livewire ^4.0 dev-dep (4.3.5 sob L13, 4.0.0 sob L12), provider no TestCase + app.key determinística, smoke test via Livewire::test → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-21913b09ac0db1f4`, 1 retry (erro de brief do maestro: classe é lyra-btn), commit `16d077e`. Maestro verificou (457/457, 1553 assertions, pint ok, dry-run L12 ok) e integrou (trail: .batuta/runs/2026-08-07-livewire-groundwork.md) (2026-08-07)
- [x] Release 0.2.0 (PR #2) mergeado (`d940987`, 2026-08-07): 16 feats da onda A publicados; config do release-please corrigida em `23a73e3` (2026-08-07)
- [x] FASE 2, ONDA A COMPLETA (13/13 estáticos em um dia): brand `9a1385f`, footer `a555b25`, nav-link `1e70ef5`, person-cell `26f5a45`, segmented-ring `079e105`, stepper `f5ea638`, toast+toast-stack `0bcd840`, page-header `dbf1732`, action-bar `fb78772`, bottom-nav `8200b7c`, navbar `a7b669b`, shell `237ac3f`, icon `91ef07b`. Suíte: 313 → 456 testes (1552 assertions). Restam os 35 interativos (bloqueados pela conversa Alpine.js) (2026-08-06)
- [x] Componente Icon (fase 2, onda A, item 13/13 — último, complex) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-07df3599ab53bdbe`, worktree `icon`, commit `91ef07b`. Dependência mallardduck/blade-lucide-icons ^2.0 (decisão do usuário) + IconRegistry com curadoria dos 79 nomes (github ainda existe no set — sem fallback). Cross-review 3 lentes: 2 aceitos (title vazio acessível errado; boolean-false passthrough) corrigidos no retry `a0aa8e6`, 2 recusados com justificativa (XSS baseline do bag; preg_replace guardado por teste). Maestro verificou (456/456, 1552 assertions, pint ok, worktree e main com install fresco) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-icon.md`) (2026-08-06)
- [x] Componente Shell (fase 2, onda A, item 12/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-989ab519ba16a91c`, worktree `shell`, commit `237ac3f`. Primeira tentativa limpa. Maestro verificou (440/440, 1501 assertions, pint ok) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-shell.md`) (2026-08-06)
- [x] Componente Navbar (fase 2, onda A, item 11/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-e6207ad1f91972ff`, worktree `navbar`, commit `a7b669b`. Primeira tentativa limpa. Maestro verificou (424/424, 1450 assertions, pint ok) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-navbar.md`) (2026-08-06)
- [x] Componente BottomNav (fase 2, onda A, item 10/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-d4cecd2adb8cb69d`, worktree `bottom-nav`, commit `8200b7c`. Fluxo de seleção JS omitido (decisão fechada); ícones via Htmlable. Primeira tentativa limpa. Maestro verificou (410/410, 1402 assertions, pint ok) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-bottom-nav.md`) (2026-08-06)
- [x] Componente ActionBar (fase 2, onda A, item 9/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-d9c24386a16575d7`, worktree `action-bar`, commit `fb78772`. Clear button omitido (decisão fechada). Primeira tentativa limpa. Maestro verificou (404/404, 1379 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-action-bar.md`) (2026-08-06)
- [x] Componente PageHeader (fase 2, onda A, item 8/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-15ad377c002e86df`, worktree `page-header`, commit `dbf1732`. Primeira tentativa limpa. Maestro verificou (395/395, 1349 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-page-header.md`) (2026-08-06)
- [x] Componentes Toast + ToastStack (fase 2, onda A, item 7/13, exceção acoplada — mesmo módulo React) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-18da8272324118f5`, worktree `toast`, commit `0bcd840`. Close button omitido (decisão fechada, precedente do Tag); role=status com override do usuário pinado. Primeira tentativa limpa. Maestro verificou (383/383, 1312 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-toast.md`) (2026-08-06)
- [x] Componente Stepper (fase 2, onda A, item 6/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-985b4f4309bf61c1`, worktree `stepper`, commit `f5ea638`. Primeira tentativa limpa. Maestro verificou (369/369, 1268 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-stepper.md`) (2026-08-06)
- [x] Componente SegmentedRing (fase 2, onda A, item 5/13, complex) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-4929db7df7bd787f`, worktree `segmented-ring`, commit `079e105`. Porte exato do algoritmo de arcos (fdiv nas bordas). Cross-review 3 lentes: 1 Medium aceito (tone desconhecido dava 500 — retry com fallback neutral + regressão), 1 Medium recusado (formatação de magnitudes patológicas), Architect/Minimalist limpos. Maestro verificou (362/362, 1228 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-segmented-ring.md`) (2026-08-06)
- [x] Componente PersonCell (fase 2, onda A, item 4/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-2294e1247d392b6d`, worktree `person-cell`, commit `26f5a45`. Primeira tentativa limpa. Scout B (deepseek) falhou 2× em silent hang (0 bytes) — pesquisa dos 6 contratos restantes assumida pelo maestro (fallback da lane). Maestro verificou (346/346, 1140 assertions, pint ok) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-person-cell.md`) (2026-08-06)
- [x] Componente NavLink (fase 2, onda A, item 3/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-69cfa8e6f085736e`, worktree `nav-link`, commit `1e70ef5`. Primeira tentativa limpa, sem retry — brief já carregava as lições dos itens 1–2. Maestro verificou (340/340, 1114 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-nav-link.md`) (2026-08-06)
- [x] Componente Footer (fase 2, onda A, item 2/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-30012cb09671289f`, worktree `footer`, commit `a555b25` (1 retry: diretivas Blade adjacentes `@endisset@isset` não compilam — diagnóstico do maestro; retry seguiu o padrão multiline do card e assertions por containment). Maestro verificou (332/332, 1079 assertions, pint ok, worktree e main) e integrou; sessão encerrada (trail: `.batuta/runs/2026-08-06-footer.md`) (2026-08-06)
- [x] Componente Brand (fase 2, onda A, item 1/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-4e24dfb3a3b8c8bb`, worktree `brand`, commit `9a1385f`. Stop condition legítima no meio: teste de frescor das guidelines exige regenerar `lyra-blade.md` a cada componente novo — Scope ampliado (gerar via `php bin/generate-boost-guidelines`; já incluído nos próximos briefs). Sessão codex morreu mid-turn de novo (6ª vez); falhas visíveis eram vendor truncado no worktree, não regressões. Maestro verificou (worktree e main: 323/323, 1045 assertions, pint ok; falha pré-existente do smoke-probe na main era vendor obsoleto — reinstalado) e integrou. Board: run lifecycle bloqueado por policy (attach negado, complete exige token) — board vira espelho passivo (trail: `.batuta/runs/2026-08-06-brand.md`) (2026-08-06)
- [x] Componente Brand (fase 2, onda A, item 1/13) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-4e24dfb3a3b8c8bb`, worktree `brand`, commit `9a1385f`. Board task `task-7e53732c7b045083` teve o lifecycle do run bloqueado por policy (attach negado) na sessão original; um novo run (`run-498057e269b039d6`) foi enfileirado e reclamado por um segundo coordinator (Claude), que monitorou a sessão codex já em andamento (TDD: commit de testes `5b452ad`, depois implementação `419ad99`) até a sessão encerrar pós-commit (padrão conhecido — 6ª vez). Nesse intervalo, um terceiro coordinator concorrente já havia verificado e integrado o mesmo trabalho em `main` (`9a1385f`) — corrida entre coordinators no mesmo task; nenhum dano, mesmo conteúdo. Maestro (este) revalidou de forma independente no worktree após corrigir vendor quebrado (`composer install`; suíte falhava 307/323 por "facade root não definido" até o install completar): pest 323/323 (1045 assertions), pint ok. Diff de `resources/boost/guidelines/lyra-blade.md` (fora do Scope declarado no brief) aceito como efeito colateral legítimo do gerador — lição registrada para briefs futuros de componente. Worktree e branch removidos, sessões encerradas (trail: `.batuta/runs/2026-08-06-brand.md`) (2026-08-06)
- [x] Renderização compacta das combinações de classe nas guidelines → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-eba44e549fd3529c`, worktree `compact-guidelines`, commit `1151556`. Fatoração só com prova de igualdade exata por expansão (`button` fatorado + 4 sobras listadas; `separator` segue como alternativas); 4 testes pinam o invariante. Cross-review: nenhum defeito de correção, 2 Low de robustez de teste rejeitados como bloqueio com justificativa no trail. Maestro verificou (313/313, 1005 assertions, pint ok, idempotente, frescor vermelho no 28º) e integrou (trail: `.batuta/runs/2026-08-06-compact-guidelines.md`). Sessão codex morreu pós-commit de novo — 5ª vez, padrão estável (2026-08-06)
- [x] Guidelines para LLMs via laravel/boost (`resources/boost/guidelines/lyra-blade.md`) → codex (gpt-5.6-sol, reasoning high) via Compozy, worktree `boost-guidelines`, commit `d9c5420`. Descoberta do boost é por convenção (`resources/boost/guidelines`), sem `extra.boost` — verificado na fonte instalada. Preâmbulo fixo + referência gerada dos `@props` e das fixtures, nada hardcoded, com teste de frescor. Cross-review pegou dois defeitos reais (documento afirmava combinações de classe impossíveis; entradas de teste vendidas como allow-list de API) e o retry corrigiu junto com parser silenciosamente errado e guardas faltando. Maestro verificou (309/309, 997 assertions, pint ok, gerador idempotente, frescor provado vermelho no 28º componente) e integrou (trail: `.batuta/runs/2026-08-06-boost-guidelines.md`) (2026-08-06)
- [x] Sintaxe curta `<lyra:*>` como alias de `<x-lyra::*>` (whitelist derivada do diretório de componentes, sempre ligada, sem config) → codex (gpt-5.6-sol, reasoning high) via Compozy, worktree `short-syntax`, commit `497809a`. Tentativa 1 entregou o código mas a sessão `sess-04a59c0a1beb319b` morreu sem report (`peer disconnected`); cross-review do Codex achou divergência real — `@verbatim`/`@php` eram mutados, quebrando a garantia do Blade — e o retry (`sess-7fe6afa45707eb5f`) corrigiu protegendo as regiões cruas e endurecendo o teste por componente (curto × namespaced nas duas formas, para os 27). Maestro verificou (297/297, 973 assertions, pint ok, 11 sondas próprias na lógica de blocos crus) e integrou (trail: `.batuta/runs/2026-08-06-short-syntax.md`) (2026-08-06)
- [x] Pós-fase 1: repo GitHub criado (lyra-ds/blade) + primeiro push; README/quickstart real (`8cf028c`, codex); suporte Laravel 12/13 com L11 derrubado por EOL de segurança (`dc6d971`, codex, emenda aplicada em retry; suíte validada sob laravel/framework 13.24) — CI verde no Actions: L12/13 × PHP 8.3/8.4, run 31065184912 (2026-08-06)
- [x] Onda 6 completa — FASE 1 DO PRD ENCERRADA (26 componentes estáticos): Pagination `a082a51` (escalada a reasoning high na 3ª tentativa: 1ª morreu no meio do TDD, 2ª escreveu fora do worktree; adaptação PRD §3 onChange→links com template {page}) e Table `9eaa7c0` — codex (gpt-5.6-sol) executou, maestro verificou (suíte final 260/260, 907 assertions, pint ok) e integrou. Worktrees e sessões limpos (2026-08-06)
- [x] Componente Breadcrumb (onda 6, item 1/3) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-ccd321b9d48a7c26`, worktree `breadcrumb`. Tentativa 1 falhou sem tocar arquivo algum (sessão tentou carregar a skill Compozy `cy-execute-task`, que não resolve a partir do worktree aninhado, e parou pela própria stop condition do brief); retry com instrução explícita para ignorar as skills Compozy e implementar direto. Maestro verificou de forma independente (pest 242/242, 839 assertions, pint ok), squash merge em `main`, sessão encerrada (trail: `.batuta/runs/2026-08-05-breadcrumb.md`) (2026-08-06)
- [x] Onda 5 completa (Forms estáticos, 8/8): Fieldset `d0fce4a` → FormRow `1f67265` (1 retry) → Input `ff61ee2` → Textarea `120abd7` → Select `5bb9522` → Checkbox `68c7c39` → Radio (integrado com checkbox flow) → Switch `5b5f14e`. Todos: codex (gpt-5.6-sol, reasoning medium) executou nos worktrees; coordinator não integrou nenhum na janela (híbrido registrado no routing); maestro verificou (suíte final 235/235, 816 assertions, pint ok) e integrou direto; sessões encerradas, worktrees removidos (briefs: `.batuta/runs/2026-08-05-*-brief.md`) (2026-08-05)
- [x] Componente Fieldset (onda 5, item 1/8) → codex (gpt-5.6-sol, reasoning medium) via Compozy `batuta/fieldset-exec`, worktree `fieldset`. Coordinator não integrou na janela de 10min; maestro verificou (pest 176/176, pint ok) e integrou direto (`d0fce4a`), sessão encerrada (brief: `.batuta/runs/2026-08-05-fieldset-brief.md`) (2026-08-05)
- [x] Componente Grid (onda 4, item 3/3 — último) → codex (gpt-5.6-sol, reasoning medium) via Compozy, worktree `grid`. Maestro verificou no worktree (pest 169/169, pint ok) e integrou direto (`5143108`), sessão encerrada (trail: `.batuta/runs/2026-08-05-grid-brief.md`) (2026-08-05)
- [x] Componente Stack (onda 4, item 2/3) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-2f3f7646ee7eff2f`, worktree `stack`. Coordinator não correlacionou sessão↔task no modo 2 (observação do usuário confirmada — sem run, sem integração automática); maestro verificou no worktree (pest 158/158, pint ok) e integrou direto (`06a607e`), sessão encerrada (trail: `.batuta/runs/2026-08-05-stack-brief.md`) (2026-08-05)
- [x] Componente Container (onda 4, item 1/3) → execução concluída pelo Claude Sonnet 5 (sessão de task do coordinator) após a sessão codex (gpt-5.6-sol) travar no meio — atribuição corrigida a pedido do usuário — via Compozy (delegação original ficou órfã em `sess-fc545a7fe6e3914a`/`sess-cc747d751844614b` após 2 recuperações do scheduler; a sessão codex `sess-cb1b1652f6f2a991` deixou o worktree pronto mas ficou parada em execução de teste sem concluir — o maestro reclamou o run em `sess-934d98a1313fc4da` e assumiu commit/integração diretamente), worktree `container`. Verificado pelo maestro (pest 148/148, incluindo o caso combinado max+class+style checado manualmente, pint ok), squash merge em `main` (`d071c78`) (trail: `.batuta/runs/2026-08-05-container.md`) (2026-08-05)
- [x] Componente Avatar (onda 3, item 5/5 — último) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-542563cebad108a4`, worktree `avatar`. Verificado pelo maestro (pest 140/140, pint ok), squash merge em `main` (`6512273`), sessão encerrada (trail: `.batuta/runs/2026-08-05-avatar.md`) (2026-08-05)
- [x] Componente EmptyState (onda 3, item 4/5) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-ba750e805b5fb901` (a sessão originalmente vinculada ao run, `sess-b28e24855c336fbe`, ficou travada em initial_bind sem progresso; o maestro delegou de novo diretamente), worktree `empty-state`. Verificado pelo maestro (pest 122/122, pint ok), squash merge em `main` (`ce7b572`), sessões encerradas (trail: `.batuta/runs/2026-08-05-empty-state.md`) (2026-08-05)
- [x] Componente Stat (onda 3, item 3/5) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-9aebf701d703dd6d`, worktree `stat`. Verificado pelo maestro (pest 117/117, pint ok), squash merge em `main` (`78236f9`), sessão encerrada (trail: `.batuta/runs/2026-08-05-stat.md`) (2026-08-05)
- [x] Componente Alert (onda 3, item 2/5) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-e57894e83f043070`, worktree `alert`. Verificado pelo maestro (pest 10/10 focado, 109/109 suíte completa, pint ok), squash merge em `main` (`e33b29c`), sessão encerrada (trail: `.batuta/runs/2026-08-05-alert.md`) (2026-08-05)
- [x] Componente Card (onda 3, item 1/5) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-0dd471e9287f9abe` (run passou por 2 recuperações do scheduler antes da confirmação do maestro), worktree `card`. Verificado pelo maestro (pest 99/99, pint ok), squash merge em `main`, sessão encerrada (trail: `.batuta/runs/2026-08-05-card.md`) (2026-08-05)
- [x] Componente IconButton (onda 2, item único) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-31026db4e623b1fc` (run recuperado uma vez pelo scheduler antes da confirmação do maestro), worktree `iconbutton`. Verificado pelo maestro (pest 87/87, pint ok), squash merge em `main` (`0937e27`), sessão encerrada (trail: `.batuta/runs/2026-08-05-iconbutton.md`) (2026-08-05)
- [x] Componente Progress (onda 1, item 6/6 — último) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-c87cac5af20eb099`, worktree `progress`. Verificado pelo maestro (pest 75/75, pint ok), squash merge em `main` (`9515c9c`), sessão encerrada (trail: `.batuta/runs/2026-08-05-progress.md`) (2026-08-05)
- [x] Componente Skeleton (onda 1, item 5/6) → codex (default model) via Compozy `sess-c31e36205c949eec` (run recuperado uma vez pelo scheduler antes da confirmação do maestro), worktree `skeleton`. Verificado pelo maestro (pest 66/66, pint ok), squash merge em `main` (`723ad24`), sessão encerrada (trail: `.batuta/runs/2026-08-05-skeleton.md`) (2026-08-05)
- [x] Componente Separator (onda 1, item 4/6) → codex (gpt-5.6-sol, reasoning medium) via Compozy `sess-6a14e1ab9c6c3674`, worktree `separator`. Verificado pelo maestro (pest 57/57, pint ok), squash merge em `main` (`970fda1`), sessão encerrada (trail: `.batuta/runs/2026-08-05-separator.md`) (2026-08-05)
- [x] Componente Tag (onda 1, item 3/6) → codex (default model) via Compozy `sess-78659027d929a624`, worktree `tag`. Verificado pelo maestro (pest 47/47, pint ok), squash merge em `main` (`85b10a7`), sessão encerrada (trail: `.batuta/runs/2026-08-05-tag.md`) (2026-08-05)
- [x] Componente Badge (onda 1, item 2/6) → codex (default model) via Compozy `sess-54c963fa20020999`, worktree `badge`. Verificado pelo maestro (pest 43/43, pint ok), squash merge em `main` (`aa0a328`), sessão encerrada (trail: `.batuta/runs/2026-08-05-badge.md`) (2026-08-05)
- [x] Componente Spinner (onda 1, item 1/6) → codex (default model) via Compozy `sess-80f49b33bedc327f`, worktree `spinner`. Verificado pelo maestro (pest 33/33, pint ok), squash merge em `main` (`a8e4624`), sessão encerrada (trail: `.batuta/runs/2026-08-05-spinner.md`) (2026-08-05)
- [x] Componente Button (conversão React → Blade, fixtures de emissão de classes) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-a5604518407d30df`, worktree `button`. Verificado pelo maestro (pest 26/26, pint ok), squash merge em `main` (`67920bb`), sessão encerrada (trail: `.batuta/runs/2026-08-05-button.md`) (2026-08-05)
- [x] Esqueleto do pacote (composer.json, service provider, Pest, Pint, CI, docs, laravel/boost em dev) → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-0b72d62cb9d9aa88`, worktree `esqueleto-pacote`. Verificado pelo maestro (pest 2/2, pint ok), merge ff em `main` (`8dda105`), sessão encerrada (2026-08-05)
