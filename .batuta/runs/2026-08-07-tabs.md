# Run trail — Tabs (fase 2, onda core, item 4/7)

- **Data:** 2026-08-07
- **Rota:** complex → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-f5b19de99cf6f7c9` (execução) + `sess-7733a4cca5ce6f0f` (retry pós-review), worktree `tabs`
- **Brief:** `.batuta/runs/2026-08-07-tabs-brief.md`
- **Commit:** `f0ebefd` (squash de 5878692 + c6e9f28)

## Ciclo

1. Research direto pelo maestro (alpine tabs.ts + react tabs.tsx + browser test canônico do plugin).
2. Decisões fechadas: root estrutural sem classe; passthrough no tablist; x-modelable="active"; estado servido com paridade do primeiro paint (modifier/aria-selected/tabindex, fallback para o 1º item); extensão documentada `panel` por item (React serve painéis vazios; cânone Alpine carrega conteúdo); onChange não portado; sem ids server-side.
3. Execução limpa na 1ª tentativa COM commit (lição do Drawer absorvida). Sessão morreu pós-commit (padrão).
4. Cross-review (3 lentes via sessão Compozy, com nota de baseline para não repetir o falso positivo do Drawer): 1 Medium ACEITO — arrays com chaves não sequenciais dessincronizavam o índice posicional do active (nenhuma aba ativa). Retry em nova sessão: `array_values` + regressão. Architect/Minimalist limpos.

## Provas reexecutadas pelo maestro

- Worktree: pest 514/514 (1903 assertions); pint passed; gerador idempotente; escopo = 4 arquivos.
- Main pós-squash: pest 514/514; pint passed; push ok.

## Veredito

Aprovado e integrado.
