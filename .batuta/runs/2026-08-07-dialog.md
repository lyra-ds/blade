# Run trail — Dialog (fase 2, onda core, item 2/7)

- **Data:** 2026-08-07
- **Rota:** complex → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-f0159cdae78c4064`, worktree `dialog`
- **Brief:** `.batuta/runs/2026-08-07-dialog-brief.md`
- **Commits:** executor `9785e77` + `f747dac`; squash final na main

## Ciclo

1. Scout deepseek 1/1 limpo (contrato React dialog.tsx + alpine dialog.ts + SSR test; âncoras verificadas, guard limpo).
2. Decisões fechadas no brief: `closable` mapeia a condição onClose do React; `labelId` opcional opta por ids server-side (default sem ids, como Dropdown); passthrough no PANEL (paridade com o spread do React); checkpoint de design do wire:model — split `wire:model*`/`x-model*` para o root (onde vive o x-modelable), resto no panel.
3. Execução limpa na 1ª tentativa (4 arquivos, TDD, 480/480), incluindo o checkpoint resolvido como proposto. Sessão sobreviveu ao turno (1ª vez).
4. **Cross-review (3 lentes, gpt-5.6-sol read-only; findings coletadas do stdout e persistidas em artefato — o sandbox segue bloqueando escrita de arquivo):**
   - ACEITA (Medium, Architect): bag do panel renderizava antes dos atributos fixos — consumidor podia sobrescrever role/x-bind (em HTML a 1ª ocorrência vence; no React os fixos vêm depois do spread e vencem). Fix: fixos antes do bag + regressão de ordem.
   - ACEITA (Low, Skeptic): `ENT_SUBSTITUTE` no escaping do labelId (UTF-8 malformado virava string vazia) + regressão.
   - Minimalist: nada.
5. Retry na MESMA sessão (contexto preservado) aplicou exatamente os 2 fixes.

## Provas reexecutadas pelo maestro

- Worktree: pest 482/482 (1707 assertions); pint passed; gerador idempotente; escopo = 4 arquivos do brief.
- Main pós-squash: pest 482/482; pint passed.

## Veredito

Aprovado e integrado. Lição de padrão consolidada: em componente interativo, TODO atributo fixo do componente renderiza antes de qualquer bag (duplicatas: primeira ocorrência vence) — já é a 2ª vez que o cross-review pega variação disso.
