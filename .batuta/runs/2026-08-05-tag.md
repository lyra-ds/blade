# Run — Componente Tag (React → Blade) — onda 1, item 3/6

**Date:** 2026-08-05 · **Lane:** medium · **Executor:** codex (default model)
**Commit:** 85b10a7 · **Verdict:** ✅ approved · **Session:** sess-78659027d929a624 · **Task:** task-978a33d66a5c76c2 (run-ec36bf2291297a9b)

## Brief
Work only inside /home/franciscpd/Projects/lyra-ds/blade/.batuta/worktrees/tag (a git worktree of lyra-ds/blade; branch batuta/tag).

## Goal

Create the `<x-lyra::tag>` Blade component mirroring the React Tag, with data-driven class-emission tests proving exact class-string parity.

## Context

The package ships `<x-lyra::button>`, `<x-lyra::spinner>`, `<x-lyra::badge>` built to this exact pattern — study and mirror:
- resources/views/components/badge.blade.php (closest sibling)
- tests/Feature/BadgeTest.php
- tests/Fixtures/class-emission/badge.json

React contract (verified against packages/react/src/tag/tag.tsx in the main repo — do NOT deviate; never invent API):

Props:
- content: React `children` → Blade default slot
- React also has `onRemove` (JS callback) + `removeLabel`, which render a remove `<button class="lyra-tag__remove">` with an embedded Icon. CLOSED DECISION for phase 1 (static, no Alpine, no bundled icon library): the remove button is NOT implemented. Add a one-line docblock comment in the view noting the omission ("onRemove/removeLabel: interactive, phase 2"). Do not invent any replacement prop.

Rendered markup (exact):
```html
<span class="lyra-tag[ user classes last]" {...passthrough attrs}>{slot}</span>
```
- Class computation: `'lyra-tag'` + user classes ALWAYS LAST, single-space joined. No modifier classes.
- No role/aria on the root; all HTML attributes pass through.

## Conventions

- PHP >= 8.3, Laravel 11/12 Blade anonymous component. Pest for tests, Pint for style (run both).
- TDD: failing tests first. Conventional commits (WIP allowed; history rewritten at integration).
- Test laws: test the behavior, never the mock; a failing test means fix the code, not the test; no test-only flags or branches in production code.

## Task

1. `tests/Fixtures/class-emission/tag.json` — cases: base (no props), user class appended.
2. `resources/views/components/tag.blade.php` — anonymous component per the contract.
3. `tests/Feature/TagTest.php` — fixture-driven exact class assertions + markup: slot content, attribute passthrough, user class last, NO remove button rendered.

## Acceptance criteria

- `vendor/bin/pest` passes (existing suite stays green).
- `<x-lyra::tag>PHP</x-lyra::tag>` → class exactly `lyra-tag`, content `PHP`, no `lyra-tag__remove` anywhere.
- `<x-lyra::tag class="x" data-id="1">Go</x-lyra::tag>` → class exactly `lyra-tag x`, `data-id="1"` present.
- `vendor/bin/pint --test` passes.

## Boundaries

Do not touch anything outside Scope. No CSS/JS, no new dependencies.

## Scope (closed list — do not change anything outside it; if the task requires it, stop and report)

- resources/views/components/tag.blade.php (new)
- tests/Fixtures/class-emission/tag.json (new)
- tests/Feature/TagTest.php (new)

## Expected evidence

Report: files touched; pest and pint runs with actual output; uncertainties declared as such.

## Stop conditions

Stop and report instead of improvising when: the code's shape contradicts this brief, the same command fails twice, or the fix needs edits beyond Scope.

## Executor report

Implemented and committed `<x-lyra::tag>` on `batuta/tag`.

Files added:

- `resources/views/components/tag.blade.php`
- `tests/Fixtures/class-emission/tag.json`
- `tests/Feature/TagTest.php`

Verification:

- Focused Pest: `OK (4 tests, 14 assertions)`
- Full Pest suite: `OK (47 tests, 149 assertions)`
- Pint: `{"tool":"pint","result":"passed"}`
- Worktree clean; generated `.phpunit.result.cache` removed.
- Commit: `73b756f feat: add tag Blade component`

Note: the worktree has no local `vendor`, so verification used the parent checkout's installed Pest/Pint binaries with worktree-specific autoload mappings. No functional uncertainties.

## Verification
- Scope check — `git diff main...batuta/tag --stat` → exactly the 3 files in the brief's closed Scope list, no drive-by edits.
- Diff review — `git diff main...batuta/tag` → component matches the contract exactly: `'lyra-tag'` base class only (no modifiers), user classes appended last via `$attributes->class(['lyra-tag'])`, no forced role/aria, phase-1 omission of the interactive remove button documented as a one-line Blade comment, no invented replacement prop.
- Tests — re-run (not trusted from executor report) inside the worktree after `composer install`: first attempt showed `resources/views/components/tag.blade.php` unexpectedly missing from the working tree (transient filesystem anomaly, not a code defect — the file was present and correct in the commit per `git show`/`git ls-tree`); restored with `git checkout -- resources/views/components/tag.blade.php`, working tree then clean, and `vendor/bin/pest` → 47 passed (149 assertions), including both fixture-driven class-emission cases and the full existing button/spinner/badge/service-provider suite (no regressions). Working tree stayed clean after the re-run.
- Lint — `vendor/bin/pint --test` → `{"tool":"pint","result":"passed"}`.
- Acceptance criteria — checked one by one: base case (`lyra-tag`, content rendered, no remove button anywhere), passthrough case (`lyra-tag x`, `data-id="1"` present) — all confirmed in the diff and test file.
- Test-hygiene scans: clean (dataset-driven fixture assertions, no test-only production flags).
- Cross-review: n/a.

## Retries and escalation
None — approved on first delegation. (One verification-time anomaly noted above: the component file was transiently absent from the worktree's working directory before the maestro's first local test run; restoring it from the commit resolved it immediately and the suite was green afterward with no residue on repeat runs.)
