# Run trail — Dropdown (fase 2, onda core, item 1/7 — primeiro interativo)

- **Data:** 2026-08-07
- **Rota:** complex → codex (gpt-5.6-sol, reasoning high) via Compozy `sess-1d654fe78f786326` (execução) + `sess-29d0305cdd1a0c9e` (retry pós-review; a sessão original morreu pós-commit — 8ª ocorrência do padrão), worktree `dropdown`
- **Brief:** `.batuta/runs/2026-08-07-dropdown-brief.md`
- **Commits:** executor `b84a992` + `550b784`; squash final na main `56f130d`

## Ciclo

1. Contrato levantado pelo maestro nas fontes (scout deepseek 1/1 limpo para o panorama; leitura direta de `dropdown.tsx` e `alpine/src/dropdown.ts` para o detalhe). Decisões fechadas no brief: trigger sempre embrulhado em span (ramo plain-content do React), sem ids server-side, `x-modelable="open"`, `x-cloak` só quando fechado, onSelect não portado.
2. Execução limpa na 1ª tentativa (4 arquivos, TDD; sessão morreu no gate final com commit íntegro).
3. **Cross-review (3 lentes, gpt-5.6-sol read-only):** o sandbox bloqueou a escrita do arquivo de findings fora do repo — findings capturadas do stdout e persistidas pelo maestro em artefato (desvio registrado; conteúdo verbatim). 5 findings:
   - ACEITA (Medium): consumidor passando `x-data` vencia o binding do componente (bag renderizava primeiro) → x-data/x-modelable movidos para antes do bag + docblock + teste de regressão de ordem.
   - ACEITA (Medium): guidelines documentavam classes de menu/item como "root class alternatives" → fixtures reduzidas à convenção root-only (precedente toast.json); menu/item ficam inline no teste; guidelines regeneradas.
   - ACEITA (Low): asserção Livewire varria a resposta inteira → apertada para a opening tag do root.
   - RECUSADA (Low): "Laravel dedupa classes quebrando paridade" — fato errado; verificado `lyra-dropdown lyra-dropdown x` (sem dedup, igual cx do React).
   - RECUSADA (Low): coerção de align mascara typo — precedente SegmentedRing (fallback seguro); React emite classe-lixo igualmente silenciosa.
4. Retry aplicou exatamente as 3 aceitas; guard de escopo ok (4 arquivos permitidos).

## Provas reexecutadas pelo maestro

- Worktree: pest 468/468 (1616 assertions); pint passed; gerador de guidelines idempotente (git limpo após 2ª execução).
- Main pós-squash: pest 468/468; pint passed.
- Scope check: diff main..branch = exatamente os 4 arquivos do Scope.

## Veredito

Aprovado e integrado (`56f130d`). Padrão Alpine estabelecido para os 6 interativos restantes da onda core.
